# Notes

Evolving architectural decisions and captured invariants for `molcfg`. Each
note is appended by `/mol:note`. Stable, frequently-referenced rules get
promoted from here into `CLAUDE.md` (the thin router) over time.

<!-- /mol:note appends entries below this line -->
